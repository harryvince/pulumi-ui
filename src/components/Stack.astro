---
type Props = {
    data: string;
    loading: boolean;
};

const { data, loading } = Astro.props;
---

<article aria-busy={loading}>{(data.trim() !== '' ) && data}</article>

<style>
  article {
    text-align: center;
    margin: 0;
  }
</style>
